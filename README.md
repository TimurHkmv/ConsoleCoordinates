# ConsoleCoordinates

Небольшая консольная утилита на C++: парсит точки из текста в формате `{:}` и рисует их на координатной плоскости в ASCII, а также выводит расстояния от первой точки и сортирует их. [file:1][file:2][file:3]

## Сборка

```bash
mkdir build
cd build
cmake ..
cmake --build . 